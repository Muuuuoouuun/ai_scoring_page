# V13 고정 점수표 독립 검산 — 2026-09-12

**검산 통과. V12→V13 점수 변화는 0이며 PASS 138 / GAP 4 / UNVERIFIED 10, `changes=[]`가 맞다.** Site·점수표·문서·Git·배포·브라우저를 수정하거나 조작하지 않았다. V12/V13 JSON·Markdown 및 V13 성능 검증 문서를 읽고 Node assertion으로 비교했다. 12개 성능 원시 표본 재검산은 다른 검토자의 범위이며 여기서는 반복하지 않았다.

## 구조·값 검증

- 두 버전 모두 9영역, 38그룹, 중복 없는 152개 ID다. 모든 그룹은 4게이트, 각 영역의 배점 합계는 정확히 100이다.
- 두 JSON의 `areas` 전체를 deep equality로 비교했다. 영역·그룹 이름과 순서, 그룹 가중치, 게이트 ID·기준 문구·가중치·상태·획득 점수·기존 게이트 evidence가 **완전히 동일**하다.
- 각 게이트의 배점이 그룹 배점÷4인지, PASS만 전액 획득하고 GAP/UNVERIFIED는 0인지 검증했다. 계산은 1/4점 정수 단위로 수행했으며 점수 반올림·가중치 변경은 없다.
- 재계산한 9개 합계 및 상태 개수가 두 JSON의 `totals`·`counts`와 각각 일치한다. 두 Markdown의 152개 게이트 행과 9개 요약도 각각 JSON과 일치하고, V12→V13 간 게이트 행은 동일하다.
- `changes=[]`, `previousChanges`, 계산 방법, baseline 및 caps 규칙은 그대로다. 기존 evidenceRegister 항목도 수정·삭제되지 않았다. 새 등록 항목은 `E13`뿐이며 기존 PASS에 추가 점수를 주지 않는다.
- JSON의 실제 차이는 현재/직전 배포 소스·시각, V13 배포/변경 설명, `overall`의 미완료 설명, 새 `evidenceRegister.E13`에 한정된다. 전체 목표는 여전히 `incomplete`다.

| 영역 | V12 = V13 | 88 이상 |
|---|---:|---|
| 정체성·기획 일치 | 86.25 | 미달 |
| 내용·최신성 | 100.00 | 통과 |
| 디자인 | 93.75 | 통과 |
| 사용성 | 95.00 | 통과 |
| 쾌적성·접근성 | 87.50 | 미달 |
| 유용성 | 86.25 | 미달 |
| 커뮤니티 | 93.75 | 통과 |
| 개인 기능 | 81.25 | 미달 |
| 배포·운영 | 93.75 | 통과 |

9영역 중 5영역만 88 이상이며 4영역이 미달한다. 수치와 별개로 기존 명시적 미완료 요구도 전체 완료를 막는다는 규칙이 유지된다.

## 성능 검증 문서의 주장·한계 점검

현재 문서에서 수정이 필요한 중대한 과장이나 미완료 범위의 은폐를 발견하지 못했다. 제목·검색의 D1 의존성 제거와 같은 환경의 소규모 홈 표본 개선을 설명하면서, 모든 지연의 원인을 D1로 단정하거나 사이트 전체 성능 해결로 확대하지 않는다.

- 실제 RSC의 RED→GREEN, 실제 source/SQLite 사용 및 ToolUI·Icons 대역 범위를 적절히 구분한다. 독립 추가 메모리 검사를 Site의 네 개 테스트에 합산하지 않고 테스트 ms/watchdog도 성능 예산에서 제외한다.
- 수정 전후 각 경로 3회, 실제 1280×720, 교대 탐색, 10초 기록, 첫 reload/후속 navigate, 늦은 observer 등록·buffered 의존·진단 버튼 영향·비통제 캐시/네트워크를 명시한다. 대기 timeout이 발생한 표본을 버리지 않았다는 설명도 유지한다.
- 홈 사후 표본의 예산 충족과 탐색 사후 TTFB/FCP/LCP 3/3 예산 초과를 함께 기록한다. 사전에도 빠른 홈 표본이 있었음, 단일 브라우저·소규모 실험임, p75/냉캐시/비회원/호스팅 전체/D1 시간으로 일반화할 수 없음을 밝힌다. 알려진 scriptEncodedBytes 0 및 unknownSizes를 비용 0이나 전체 전송 예산 충족으로 바꾸지 않는다.
- 로컬 공개 QA 후기와 운영 빈 커뮤니티를 구분한다. 인위적으로 멈춘 브라우저 pending→최종 전환을 직접 촬영하지 못했으며 RSC·최종 화면 관찰로 이를 대체하지 않았다고 명시한다. 해당 기록의 CLS 0도 긴 글·작은 폭·늦은 전환 전체의 안정성을 보장하지 않는다.
- info 수준 canceled 로고 요청을 오류 없이 지나간 관측으로 삭제하지 않고, reportedError/statusCode null 및 원인 미확정을 보존한다. 새 운영 후기/피드백 응답을 만든 것으로 서술하지 않는다.
- 실제 OS 확대·VoiceOver 미실시, 탐색 지연, 외부 실제 과업·독립 실제 계정 격리 관찰·전체 확장 상태·정기 실행·실제 이메일·발행 운영 등 미완료를 유지하며 전체 목표를 진행 중으로 표시한다. 나머지 14개 미통과 게이트의 원문은 연결된 점수표에 그대로 보존되어 있다.

5.3.1~5.3.4의 기존 문구는 각각 측정 수행을 요구하는 게이트이고 이미 PASS였다. 이번 관측으로 새 점수를 더하지 않는 해석이 점수표와 일치한다. 성능 예산 초과가 사라졌다거나 모든 상호작용·접근성이 검증됐다는 판정은 하지 않았다.

배포 정보는 root 전달 및 검토 문서에서 서로 일치한다: 소스 `ac64bc580e198ca8e7a3f7342752a5e28a6b097e`, Sites 버전 15, `2026-09-12T13:13:28.082461Z` succeeded, 환경 revision 2, 소유자 비공개. 이 검토자가 새로 배포 도구나 운영 브라우저를 실행한 증거는 아니다.

## 검토 시 파일 SHA256

| 파일 | SHA256 |
|---|---|
| `2026-09-12-score-worksheet-v12.json` | `7191f136dc4abc2da8055b2499a850333a34aa1d9181178da17d8c2925bfb7ae` |
| `2026-09-12-score-worksheet-v12.md` | `3eac2969fcf96f01744bef22c34467c701760405be59bc3cf74a3ca414b9f759` |
| `2026-09-12-score-worksheet-v13.json` | `4ea8067ca40b1fe7289b52262f68a90b28eeb419c9041984be477e932de4722a` |
| `2026-09-12-score-worksheet-v13.md` | `1a14f585b5cdb0b76109129e57b22f0652c49ec8e8c5dca6094f41091756a9f2` |
| `2026-09-12-v13-performance-verification.md` | `5e86351fe1e18d6ef30e1789cdbaf966dc510744f319d88540f2e4ea18cc3efd` |

위 다섯 파일의 기준 디렉터리는 `/Users/bigmac_moon/dev/ai_score/docs/audits/`다.
