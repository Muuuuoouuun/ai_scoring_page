# V10 배포·결제 보완 이미지 99–104 독립 검토

`rg --files`로 확인한 정확한 파일명은 모두 아래와 같이 `.png.png`로 끝난다. 원본 PNG 6장을 각각 `view_image({detail:"original"})`로 실제 열었으며, 사이트·브라우저·소스·운영 기록을 조작하지 않았다. 부모가 전달한 배포 식별은 **Sites 버전 11, 소스 `dc4d41afd8b9eeeec5602e0b23e464befbd3fad8`**이고, 이번 검토가 배포 도구로 이를 새로 확인한 것은 아니다.

## 기록 연결

이미지와 연결한 파일은 `/Users/bigmac_moon/dev/ai_score/docs/audits/v10-screens/observations.jsonl`이다. 6장 모두 요청 viewport 390×844, 기록상 문서 `client=375`, `scroll=375`, `loading=[]`이며 `fullPage` 필드는 없다; 실제 이미지는 해당 스크롤 위치의 부분 화면이다.

| 정확한 PNG 파일명 | 환경·경로 | UTC 시각 (2026-09-12) |
|---|---|---|
| 99-payment-settlement-mobile-viewport.png.png | localhost:5173/my?tab=subscription | 11:16:56.777 |
| 100-production-news-mobile.png.png | ais-discovery-hub.aaahaaah19.chatgpt.site/news | 11:17:26.754 |
| 101-production-detail-anchor-mobile.png.png | ais-discovery-hub.aaahaaah19.chatgpt.site/tools/notion#overview | 11:17:33.487 |
| 102-production-long-search-mobile.png.png | ais-discovery-hub.aaahaaah19.chatgpt.site/explore | 11:17:56.748 |
| 103-production-admin-table-left-mobile.png.png | ais-discovery-hub.aaahaaah19.chatgpt.site/admin | 11:18:23.267 |
| 104-production-admin-table-right-mobile.png.png | ais-discovery-hub.aaahaaah19.chatgpt.site/admin | 11:18:30.607 |

이미지 기준 경로는 `/Users/bigmac_moon/dev/ai_score/docs/audits/v10-screens/`이다. 위 문서 폭 기록과 아래 실제 시각 관찰은 서로 보완하지만 동일한 검증은 아니다.

## 직접 읽은 화면

**99 — 실제 정산금액과 포함 수수료, 로컬 결제 폼.** 펼친 ‘카드에 실제 정산된 금액 (선택)’ 구역에서 정산 통화 KRW, 실제 정산 총액·수수료 포함 20,000, 총액에 포함된 수수료 메모 0이 라벨과 함께 읽히고, 수수료를 다시 더하지 않는다는 설명이 분명히 표시된다. 개인 금액의 미확인·0원 구분 안내, 초점 테두리가 있는 수수료 입력, ‘V10 로컬 검증용 결제’ 메모까지 모달 안에 들어와 이 구간의 이전 72 판독 공백을 보완한다. 이미지 위쪽은 다른 입력의 중간, 아래쪽은 메모 도중에서 끝나므로 전체 결제 폼이나 저장 완료·계산 결과를 대신 검증하지 않는다.

**100 — 배포 뉴스 모바일.** 로컬 89와 같은 제목·갱신 링크·탭·분류·안내·ChatGPT 뉴스의 순서와 정렬이 실제 배포 화면에서도 보이며, 제목과 설명이 열 안에서 줄바꿈되고 저장 버튼이 겹치지 않는다. 문서 폭 375/375 기록과 함께 보이는 범위에서 가로 넘침은 없고, 작은 제품·날짜 정보도 판독되지만 목록 전체 하단을 다시 확인한 것은 아니다.

**101 — 배포 Notion overview 앵커.** 실제 이미지에서 ‘어떤 도구인가요?’ 제목과 설명은 고정 헤더 아래 여백을 두고 완전히 드러나므로 이전 헤더와 앵커 제목이 겹치던 상태가 이 배포 캡처에서는 보이지 않는다. 이어지는 기능 표의 기능명·지원 상태·조건·공식 근거 링크도 세 열 안에서 구분되며, 좁은 첫 열의 어절 분리는 일부 남지만 텍스트가 셀 밖으로 흐르거나 겹치지 않는다. 부모의 별도 실제 측정은 target top 99.953px, header 64px로 약 35.953px의 간격이며, 해당 숫자는 이 PNG의 픽셀을 추정해 만든 값이 아니다.

**102 — 배포 긴 검색어.** 입력창 아래 ‘0개 결과’와 반복된 긴 QA 검색어가 여러 줄로 감싸지고, 그 뒤 목적 관련도 안내와 빈 결과·조건 초기화 행동이 문서 폭 안에 정렬된다. 입력 자체는 한 줄의 가로 입력 영역으로 보이지만 결과 메타데이터가 페이지를 넓히지 않아 이전 01의 가로 넘침 문제가 이 배포 상태에서는 재현되지 않으며, `client=scroll=375` 기록이 이를 뒷받침한다. 이는 검색 결과의 빈 상태 레이아웃 확인이며 검색 알고리즘 정확성이나 모든 문자열 조합의 보증은 아니다.

**103 — 배포 관리자 표 왼쪽.** 이전 59에서 없던 ‘표를 좌우로 이동해 나머지 열을 확인하세요.’ 안내와 실제 가로 스크롤바가 보이고, 표 왼쪽의 동기화 시작·상태를 읽을 수 있다. 오른쪽 집계 열은 현재 위치에서 보이지 않지만 다음 행동을 알 수 있어 이전의 수평 접근 발견 가능성 공백을 보완하며, 이메일 발송은 여전히 발송 내역 없음으로 표시된다.

**104 — 배포 관리자 표 오른쪽.** 초점이 있는 표에 파란 테두리가 표시되고 스크롤바 손잡이가 오른쪽으로 이동했으며, 상태·확인·변경·실패 열과 행별 수치가 실제로 드러난다. 103과 합치면 표 양쪽 정보의 노출을 확인할 수 있고, 부모가 별도로 측정한 ArrowRight 12회 후 region client 339 / scroll 520 / left 181 / focused true는 `520−339=181`의 오른쪽 끝 위치와 이미지 상태에 부합한다. 키 입력은 제가 새로 실행한 것이 아니며, 표에서 읽히는 운영 상태와 앞선 QA 신고의 잔존 기록을 새 실사용자 참여나 이번 검토의 관리 조치 실행으로 바꾸어 기록하지 않는다.

## 해소된 공백과 남는 한계

- 99는 **실제 정산 총액·포함 수수료·중복 가산하지 않는 설명**의 모바일 판독 공백을 보완한다. QA 결제 편집의 이 구간은 읽혔지만 전체 모달 하단과 저장 동작을 보았다는 뜻은 아니다.
- 100–102는 앞서 로컬에서 확인한 뉴스 정렬·앵커 여백·긴 검색 결과 줄바꿈이 실제 배포에서도 보인다는 추가 근거다.
- 103–104는 **이전 59 관리자 내부 표의 수평 접근·안내 관찰 공백을 보완**한다. 직접 본 안내·스크롤바·표 양끝과 부모가 전달한 키보드/region 수치는 출처를 나누어 기록했다.
- 부모는 배포 초기 DOM에서 관리 로딩 status 문구를 관찰한 뒤 완성 목록이 성공적으로 나타났다고 보고했다. 이번 PNG는 이미 완성된 표의 스크롤 구간이므로 로딩 전환 자체를 제가 직접 이미지로 확인했다고 주장하지 않는다.
- **전체 모달 하단과 83의 작은 저장 조건·세부 경고의 판독 공백은 그대로 남는다.** 99는 결제 폼의 다른 구간이므로 그 공백을 대신 채우지 않는다.

현재 여섯 이미지의 보이는 영역에서 새 중대한 시각 오류는 확인하지 못했다. 주 행동·정보 위계·파란 초점 표현은 기존의 차분한 톤과 이어지며, 이 보충검토는 점수나 게이트 PASS를 추가하지 않는다.
