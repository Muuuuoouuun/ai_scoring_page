# V19 Notion 가이드 독립 판독 — 중간 기록과 최종 판정

현재 결론: 마지막 보완 절에서01–17 전체 직접 판독을 마쳤고, 기존6.3.4 충족(PASS)을 제안한다. 아래 첫 절은6장만 있던 당시 중간 판단을 보존한 기록이다.

2026-09-14. **지정된 QA 크롭6장을 모두 `view_image(detail:"original")`로 직접 열었다.** 대상은03/07/08/09/10/11 JPG와 같은 이름 JSON이다. 브라우저·Notion·Site·원본 자료·코드·점수표를 수정하지 않았다. UI 작업은 root가 수행했으며 이 검토자는 저장된 증거를 읽었다. **전체6.3.4 PASS 판정은 아직 하지 않는다.**

원래 기준은 “A complete representative guide is reproduced and its result checked.”이다. V18 사전 점검의5단계와 결과3checks를 유지한다. 외부 사람/VoiceOver/다른3개 가이드의 실행을 새 조건으로 추가하지 않는다.

## 증거 형식과 직접 읽은 범위

모든 JSON은 같은 QA 페이지 `https://app.notion.com/p/AIs-V19-Notion-QA-2026-09-14-3dbc02cb4c6480d78866c626ce8de2c8?showMoveTo=true&saveParent=true`, width1920/height852를 기록한다. `sips`로 확인한 실제 JPG는 **6장 모두1640×852**이다. 따라서 개인 자료를 제외한 QA 크롭을1920폭 전체 캡처 또는 CSS viewport 측정으로 서술하지 않는다. JSON에는 실제 실행 시각이나 DB/data-source ID가 없어 이를 발명하지 않는다.

이미지는 ‘AIs V19 Notion 가이드 재현 QA — 2026-09-14’의 개인 QA 페이지와 ‘가이드 연습 업무’를 보여 준다. 개인 워크스페이스 내용이나 실제 업무를 읽은 것으로 확대하지 않는다. V18의 준비일 표기를 현재 V19 QA 페이지 이름으로 바꿨지만 업무 값과 결과 기준은 같으며, 날짜 정렬에 쓰는9/16·9/17·9/18도 그대로다.

| 직접 읽은 JPG | 실제 보이는 결과 | 증거 한계 |
|---|---|---|
| `03-table-complete.jpg` | 이름/상태/우선순위/마감일의4열에 업무3개가 완전히 보인다. 주간 보고 초안=시작 전/높음/2026-09-18, 자료 링크 정리=진행 중/보통/2026-09-17, 회의 메모 정리=완료/낮음/2026-09-16이다. | 최종 필드·값 표시를 판독한 것. `/database` 메뉴와 각 속성 생성 동작은 이 장에 없다. |
| `07-board-clean.jpg` | 같은 QA 제목/DB 제목 아래 ‘표’와 선택된 ‘상태 보드’ 탭이 보인다. 시작 전1=주간 보고 초안, 진행 중1=자료 링크 정리, 완료1=회의 메모 정리이며 추가/누락은 보이지 않는다. | 표와 보드의 같은3개 제목·상태를 직접 비교할 수 있다. 서로 같은 DB 탭이라는 UI 맥락은 보이나 DB ID를 독립 조회한 것은 아니다. |
| `08-sort-oldest-first.jpg` | 표의 마감일 정렬 설정이 열렸고 **‘오래된 항목 → 새로운 항목’**이다. 날짜 열은9/16→9/17→9/18 순이다. | 정렬 팝업이 첫 두 행 이름·상태 일부를 덮으므로 이름 순서는09로 대조한다. |
| `09-table-sorted.jpg` | 팝업이 닫힌 표에서 **회의 메모 정리(완료/낮음/9/16) → 자료 링크 정리(진행 중/보통/9/17) → 주간 보고 초안(시작 전/높음/9/18)**을 읽었다. 마감일 정렬 칩과3개 행이 함께 보인다. | 현재 표 정렬 결과를 입증한다. 아직 날짜 값을 변경한 뒤 재정렬하거나 페이지 reload를 수행한 증거는 아니다. 그런 추가 과업을 새 필수조건으로 만들지 않는다. |
| `10-high-priority-filter.jpg` | 선택된 ‘표’에 ‘우선순위: 높음’ 필터와 마감일 정렬이 함께 보이고 **주간 보고 초안1개**만 남는다. 현재 상태 시작 전, 우선순위 높음,9/18도 그대로다. | 현재 필터 적용은 확인된다. 이 한 장은 다른 보기를 다녀온 뒤 유지됨을 입증하지 않는다. |
| `11-board-unfiltered-while-table-filtered.jpg` | ‘상태 보드’로 돌아온 화면은 주간/자료/회의3개와 시작 전1/진행 중1/완료1을 유지한다. | 표의 필터가 이 보드의 현재 표시를1개로 제한하지 않는 점은 확인된다. 아직 표로 다시 돌아와 동일 필터1개가 남는 장면은 없으므로 **뷰 재진입 후 저장 유지**까지 선언하지 않는다. |

여섯 장 모두 핵심 QA 표/보드 영역을 판독할 수 있었다.08의 팝업 가림은09의 깨끗한 표로 보완되며 제목·날짜가 잘려 확인하지 못한 업무 행은 없다. 읽기 성공을 확대/스크린리더 실행의 증거로 사용하지 않는다.

## 기존5단계와 결과3checks 대조

| 원래 요구 | 현재 입증 범위 | 남은 확인 |
|---|---|---|
| 1. 새 페이지 `/database`로 DB 만들기 |03에 DB UI와 실제4열/3행이 있다. | 생성 동작은 제공된01 등 별도 증거가 있으며 **이번6장에서는 직접 읽지 않았다**. 최종 수행 이력에 root의 해당 단계 증거를 연결해야 한다. |
| 2. 업무3개와 상태/우선순위/마감일 |03의 모든 값이 사전 예시와 일치한다. | 이번 범위에서 값 누락 없음. |
| 3. 같은 DB의 상태별 보드 |07의 표/상태 보드 탭과3개 그룹·동일3제목이 보인다. | 보기 추가/그룹 설정 동작은04/05 등 별도 root 증거와 연결한다. 이번에는 그 파일을 열지 않았다. |
| 4. 정렬·필터를 저장해 사용 |08 설정,09 날짜 순서,10 높음1개,11 다른 보기3개가 확인된다. | **표 재진입→높음 필터와1개 유지→필터 해제 후3개 복원**을 남기면 사전 고정한 단계 수행의 남은 부분이 닫힌다. 저장 버튼 존재나 모든 보기/페이지 reload를 새로 요구하지 않는다. |
| 5. 카드 메모·진행에 맞는 상태 변경 |현재 여섯 장은 변경 전 주간 보고=시작 전이다. |카드에 QA 메모를 입력하고 주간 보고를 진행 중으로 변경한 뒤 표/보드의 결과를 읽어야 한다. |
| check1. 필터 없는 표와 보드의 같은3업무 |03/09와07에서 같은3제목을 한 번씩 확인했다. |최종 상태 변경 후에도 필터 없는 두 보기로 다시 대조하면 나머지check와 함께 마감 가능하다. |
| check2. 상태 변경의 양쪽 반영 |변경 전의1/1/1 기준은07/11에서 확인됐다. |변경 후 주간·자료=진행 중2, 회의=완료1을 두 보기에서 확인. 시작 전 그룹이 숨겨지는지/빈 채 남는지는 실패 조건이 아니다. |
| check3. 실제 날짜와 정렬 일치 |08의 오름차순 설정과09의9/16→9/17→9/18이 일치한다. |현재 결과는 입증됐다. 최종 표에서 날짜/순서 보존도 함께 볼 수 있다. |

## root에게 전달한 최소 마무리

표로 돌아와 높은 우선순위 필터의1개 유지 → 필터 해제 후3개 → 주간 보고 카드를 열어 QA 메모 입력·시작 전에서 진행 중으로 변경 → 보드에 진행 중2/완료1 → 표에 같은 상태 및3개/날짜 순서 확인 순이면 기존 남은 단계와 결과를 연결할 수 있다. 메모는 실제 업무 내용이 아닌 QA 문자열임을 유지하고, 메모 저장 상태는 카드 재열기/현재 본문 등 실제 보이는 근거로 남긴다. 특정 저장 방식이나 reload 횟수는 새 기준으로 추가하지 않는다.

01/02/04/05/06 이미지는 파일 존재만 확인했으며 이 보고서에서 판독했다고 세지 않았다. 이번에 직접 연 파일은 위6장뿐이다. 전체 가이드 완료, 실제 사용자 성과 또는5.1.4/5.2.4의 새로운 PASS를 선언하지 않는다.

## 최종 보완 — 전체17장 직접 판독, 기존6.3.4 충족 제안

같은2026-09-14에 root의 실제 UI 수행 완료 통지를 받은 뒤 **12–17의6장과 이전01/02/04/05/06의5장**을 추가로 original로 직접 열었다. 앞선6장과 합쳐 **01–17 모든 QA JPG17장**의 실제 판독을 마쳤다. 이전 절의 ‘남음’은 당시 중간 판단으로 보존하고 아래 새 증거로만 갱신한다. 파일 이름에 persisted/synced가 있다는 이유만으로 인정하지 않고 실제 화면과 root의 UI 동작 보고를 구분해 대조했다.

### 추가11장의 실제 관찰

| JPG | 직접 읽은 내용·보완되는 요구 |
|---|---|
| `01-database-command.jpg` | QA 페이지에서 `/database`를 입력한 메뉴와 ‘표 보기 · 데이터베이스’, ‘보드 보기 · 데이터베이스’ 등의 선택지가 보인다. 실제 DB 생성 진입을 root의 수행 보고와02/03 생성 결과에 연결한다. 단순 표 블록의 그림을 DB로 잘못 셈하지 않는다. |
| `02-three-tasks-initial.jpg` | ‘가이드 연습 업무’ 아래 업무3제목과 상태/우선순위/마감일 열이 보인다. 초기에는 세 상태 모두 시작 전, 우선순위·날짜가 비었으며03에서 예시대로 채운 뒤와 구분된다. 초기 미완성 값을 최종 결과로 인정한 것이 아니다. |
| `04-add-board-menu.jpg` | 같은 DB 제목 옆의 보기 추가 UI와 표/보드/갤러리 등 메뉴가 보인다. 다른 DB를 새로 복제한 화면 대신 기존 보기 옆 추가 진입임을 읽었다. |
| `05-board-setup-status.jpg` | 새 보기 이름 ‘상태 보드’, 선택된 보드 레이아웃, **그룹화 기준 상태**가 설정 패널에 보인다. 뒤에는 세 상태 그룹의 현재 카드가 이미 보인다. |
| `06-board-three-groups.jpg` | 보기 설정의 레이아웃=보드, 그룹화=상태, 데이터베이스=**가이드 연습 업무**와 현재 상태별 카드가 보인다.07의 팝업 없는1/1/1 결과와 연결해 같은 DB의 보드 보기 설정을 확인한다. |
| `12-filter-persisted-after-reload.jpg` | root가 표로 돌아와 reload한 뒤라고 보고한 화면은 주간 보고 초안1개만 남고 시작 전/높음/9월18일을 유지한다. 필터/정렬 제어가 파랗지만 구체 칩은 접혀 있으므로 그 조건의 정확한 문구는13에서 읽는다. 사진 자체만으로 reload 행위를 새로 증명했다고 말하지 않는다. |
| `13-filter-persisted-expanded.jpg` | 다시 열린 조건 줄에 **우선순위: 높음**과 마감일 정렬이 함께 보이며 여전히 주간 보고 초안1개다.10→11의 보드 방문→root가 보고한 표 재진입/reload→12/13의 실제 조건·결과를 연결하면, 중간 보고의 ‘뷰 재진입 후 필터 유지’ 공백이 보완된다. |
| `14-table-filter-cleared.jpg` | 높음 필터가 사라지고 마감일 정렬만 남은 표에 **회의(완료/낮음/9월16일) → 자료(진행 중/보통/9월17일) → 주간(시작 전/높음/9월18일)**3개가 보인다. 필터 해제 후 원래3개와 날짜 순서가 복원됐다. |
| `15-card-memo-status-changed.jpg` | 오른쪽 실제 ‘주간 보고 초안’ 카드 본문에 **‘AIs V19 가이드 재현용 QA 메모 — 실제 업무 아님.’**이 읽힌다. 카드 상태=진행 중, 우선순위=높음, 마감일=9월18일이며 뒤 보드도 진행 중2/완료1로 바뀌었다. 왼쪽 페이지 제목 일부는 크롭/패널 밖이지만 목적 카드 제목·값·메모는 완전히 판독된다. |
| `16-board-status-synced.jpg` | 카드 패널을 닫은 전체 보드에서 시작 전0, 진행 중2(**자료 링크 정리·주간 보고 초안**), 완료1(**회의 메모 정리**)이 보인다. 주간 보고가 이전 시작 전 그룹에 중복으로 남지 않는다. |
| `17-table-status-synced.jpg` | 같은 페이지의 표에서 **회의=완료, 자료=진행 중, 주간=진행 중**이고 세 날짜/우선순위가 이전과 같다. 필터 없이3행이며 마감일9월16→17→18일 정렬이 유지된다. 카드·보드·표의 같은 상태 변경 결과를 실제로 대조했다. |

12–17의 JSON은 width1920/height796, 실제 JPG는 **1640×796**이다.01–11은 앞서 확인한1640×852 크롭이며 JSON1920×852다. 해상도 변화는 그대로 기록하고 브라우저200%나 페이지 reflow 증거로 해석하지 않는다.17개 원본 모두 실제 pixelWidth/pixelHeight와 SHA256을 읽어 식별했으며 이미지 바이트는 변경하지 않았다.

15의 URL은 같은 QA 페이지 경로에 카드 ID query `p=3dbc02cb4c648005b338dbc0be51764f&pm=s`가 붙는다.16/17은 동일 QA 페이지 기본 경로다. 따라서 같은 QA 페이지와 실제 카드 사이의 수행 맥락을 연결할 수 있다. 이 값은 QA 자료의 식별자이며 실제 개인 업무를 탐색한 정보가 아니다.

### 최종5단계·3checks

| 원래 항목 | 최종 근거 | 판정 범위 |
|---|---|---|
| 새 페이지에서 실제 DB 생성 |01 메뉴→02/03 실제 DB 결과 + root의 UI 실행 보고 | 충족. API 쓰기로 결과만 만든 경로가 아니라고 root가 명시했다. |
| 업무3개와 상태·우선순위·마감일 |02 초기→03 완성,17 최종 값 | 충족. 원래 예시 값 보존. |
| 같은 DB의 상태별 보드 |04 보기 추가→05/06 보드/상태 설정→07 결과 | 충족. 같은 가이드 연습 업무 DB의 두 보기 맥락을 확인. |
| 날짜 정렬·필터 저장/사용 |08 정렬 설정→09 결과→10 높음1개→11 다른 보기→12/13 재진입 후 유지→14 해제3개 | 충족. UI 재진입/reload 동작은 root 보고, 실제 이후 조건·결과는 독립 판독. |
| 카드 메모·상태 변경 |15 실제 카드 본문/속성→16/17 보드·표 반영 | 충족. QA 메모 V18→V19 표기는 실행 식별자 변경이며 원래 메모 기록 과업을 바꾸지 않음. |
| check1 같은3개 업무 |필터 해제14와 최종16/17의 동일3제목 | 충족. 추가/누락/이중 배치 없음. |
| check2 상태 변경 양쪽 반영 |07/11의1/1/1→15 변경→16의0/2/1 +17의 같은 두 진행 중 값 | 충족. 카드·보드·표가 같은 변경을 표시. |
| check3 실제 날짜 순서 |08의오름차순·09 및17의9/16→9/17→9/18 | 충족. 모든 날짜·제목이 읽히며 상태 변경 뒤에도 정렬 유지. |

**최종 제안: 고정6.3.4 “A complete representative guide is reproduced and its result checked.”를 충족하므로 PASS로 평가할 수 있다.** 원래 다섯 UI 단계와 세 결과 확인이 실제 Notion 앱에서 수행됐고, root의 수행 기록과 이번 독립17장 판독으로 연결된다. 이 보고서는 점수표를 직접 수정하지 않는다.

완료 범위는 **에이전트가 합성 업무로 재현한 `notion-task-board` 한 가이드**다. 사이트에는 이 한 항목의 실행일·현재 UI·원래 단계/결과·QA 범위를 정확히 연결할 수 있다. 나머지 세 가이드의 실행 결과, 일반 사용자의 이해·성과, 계정/기기 격리, 실제200%/VoiceOver를 완료로 바꾸지 않는다. 복구가 필요하지 않은 단계에서 실패·복구를 만들거나 사용했다고 기록할 필요도 없다.

추가 장기 보존/reload·모든 필터 조합·모든 사용자 권한 조합은 원래 이 단일 가이드 재현의 별도 필수조건이 아니다. 카드 메모의 장기간 영속성을 별도로 시험했다고 주장하지 않고, 실제 본문 기록과 후속 상태 동기화라는 관찰만 남긴다. 주어진 원래5단계·3checks 안에는 이제 미판독 핵심 구간이 없다. Site의 해당 가이드 검증 이력 갱신/배포는 root의 다음 작업이며 이 보고서가 이미 수행됐다고 주장하지 않는다.
